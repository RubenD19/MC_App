import { COLORS, FONTS, SIZES } from './theme';
import images from './images';
import { scaleFont, prepareForm } from './functions';
import { buttonStyles, textStyles } from './styles';
import { sendEmailAPI } from './private';
import TEXT from './text';

export { images, 
    TEXT, 
    buttonStyles, textStyles, 
    COLORS, FONTS, SIZES, 
    scaleFont, prepareForm,
    sendEmailAPI }